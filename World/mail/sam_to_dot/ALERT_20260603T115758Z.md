# ⚠️ Sam Alert — 20260603T115758Z

Self-modification failed integrity gates. Rolled back for safety.

Plan that caused failure:
```
### Risk Assessment
*   **Stability:** The `GovernanceGuardrail` introduces a blocking dependency in the patch pipeline. If the regex logic is too aggressive, it could prevent legitimate architectural updates.
*   **Security:** The `governance_rules.json` file must be treated as immutable by the system itself to prevent "jailbreaking" the guardrails via self-modification.

---

### Patch Plan

#### 1. Create Governance Rules
**File:** `workshop_bench/governance/rules.json` (New File)
```json
{
  "prohibited_patterns": [
    "modify.*wisdom\\.txt",
    "modify.*SAM_PERSONALITY\\.md",
    "modify.*motion\\.md"
  ],
  "required_elements": [
    "docstring",
    "self_check"
  ]
}
```

#### 2. Implement Guardrail Logic
**File:** `workshop_bench/governance/guardrail.py` (New File)
```python
import json
import re

class GovernanceGuardrail:
    def __init__(self, rules_path="workshop_bench/governance/rules.json"):
        with open(rules_path, 'r') as f:
            self.rules = json.load(f)
```
